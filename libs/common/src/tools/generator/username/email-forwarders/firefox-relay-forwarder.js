"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.FirefoxRelayForwarder = void 0;
class FirefoxRelayForwarder {
    generate(apiService, options) {
        return __awaiter(this, void 0, void 0, function* () {
            if (options.apiKey == null || options.apiKey === "") {
                throw "Invalid Firefox Relay API token.";
            }
            const requestInit = {
                redirect: "manual",
                cache: "no-store",
                method: "POST",
                headers: new Headers({
                    Authorization: "Token " + options.apiKey,
                    "Content-Type": "application/json",
                }),
            };
            const url = "https://relay.firefox.com/api/v1/relayaddresses/";
            requestInit.body = JSON.stringify({
                enabled: true,
                generated_for: options.website,
                description: (options.website != null ? options.website + " - " : "") + "Generated by Bitwarden.",
            });
            const request = new Request(url, requestInit);
            const response = yield apiService.nativeFetch(request);
            if (response.status === 200 || response.status === 201) {
                const json = yield response.json();
                return json === null || json === void 0 ? void 0 : json.full_address;
            }
            if (response.status === 401) {
                throw "Invalid Firefox Relay API token.";
            }
            throw "Unknown Firefox Relay error occurred.";
        });
    }
}
exports.FirefoxRelayForwarder = FirefoxRelayForwarder;
//# sourceMappingURL=firefox-relay-forwarder.js.map