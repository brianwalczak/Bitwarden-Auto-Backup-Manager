"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.AnonAddyForwarder = void 0;
class AnonAddyForwarder {
    generate(apiService, options) {
        return __awaiter(this, void 0, void 0, function* () {
            var _a, _b, _c;
            if (options.apiKey == null || options.apiKey === "") {
                throw "Invalid addy.io API token.";
            }
            if (((_a = options.anonaddy) === null || _a === void 0 ? void 0 : _a.domain) == null || options.anonaddy.domain === "") {
                throw "Invalid addy.io domain.";
            }
            if (((_b = options.anonaddy) === null || _b === void 0 ? void 0 : _b.baseUrl) == null || options.anonaddy.baseUrl === "") {
                throw "Invalid addy.io url.";
            }
            const requestInit = {
                redirect: "manual",
                cache: "no-store",
                method: "POST",
                headers: new Headers({
                    Authorization: "Bearer " + options.apiKey,
                    "Content-Type": "application/json",
                    "X-Requested-With": "XMLHttpRequest",
                }),
            };
            const url = options.anonaddy.baseUrl + "/api/v1/aliases";
            requestInit.body = JSON.stringify({
                domain: options.anonaddy.domain,
                description: (options.website != null ? "Website: " + options.website + ". " : "") +
                    "Generated by Bitwarden.",
            });
            const request = new Request(url, requestInit);
            const response = yield apiService.nativeFetch(request);
            if (response.status === 200 || response.status === 201) {
                const json = yield response.json();
                return (_c = json === null || json === void 0 ? void 0 : json.data) === null || _c === void 0 ? void 0 : _c.email;
            }
            if (response.status === 401) {
                throw "Invalid addy.io API token.";
            }
            if ((response === null || response === void 0 ? void 0 : response.statusText) != null) {
                throw "addy.io error:\n" + response.statusText;
            }
            throw "Unknown addy.io error occurred.";
        });
    }
}
exports.AnonAddyForwarder = AnonAddyForwarder;
//# sourceMappingURL=anon-addy-forwarder.js.map